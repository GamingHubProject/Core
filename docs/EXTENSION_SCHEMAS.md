# Registry and package schemas

See `examples/extensions.json`, `examples/gaming-hub-extension.json`, and `examples/SHA256SUMS`. Remote text is escaped and raw HTML is rejected. The package manifest and Azuriom `plugin.json` identifiers and versions must agree. Compatibility is evaluated with Composer Semver when available.
